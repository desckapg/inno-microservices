spring:
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true

eureka:
  client:
    enabled: false

management:
  logging:
    export:
      enabled: false
  tracing:
    export:
      enabled: false
  otlp:
    metrics:
      export:
        enabled: false